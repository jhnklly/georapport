geo-reapportion aka georapport aka transfer attributes aka two themes
john@greeninfo.org
2014-05-26

http://websites.greeninfo.org/greeninfo/labs/georapport

USE CASE:
We have populations and median incomes in census (tract/block) geometries, but we need them in custom geometries (school districts, POI buffers, neighborhoods, etc.). Special case where custom geoms overlap (e.g. some POI buffers).


